# Codex Entry Point

- Follow `../AGENTS.md` as the canonical repository policy.
- DaisyUI UI work requires the project-configured `daisyui-blueprint` MCP; if its tools are unavailable, restart Codex from this project and stop if initialization still fails.
- For complex work, follow `../plans/README.md` and maintain the relevant plan.
- Use `../REVIEW.md` for review-only tasks.
- Do not recreate removed legacy assistant surfaces such as `.claude/`, `CLAUDE.md`, `.cursorrules`, or `.windsurfrules` unless explicitly requested.
