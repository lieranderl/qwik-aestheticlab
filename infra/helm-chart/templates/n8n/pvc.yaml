kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-n8n-pvc
spec:
  storageClassName: hcloud-volumes
  volumeName: {{ .Release.Name }}-n8n-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi