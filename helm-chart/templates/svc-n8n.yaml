apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc-n8n
spec:
  type: ClusterIP
  ports:
    - port: 5678
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-n8n
