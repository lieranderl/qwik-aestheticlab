apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-n8n-svc
spec:
  type: ClusterIP
  ports:
    - port: 5678
      targetPort: 5678
      protocol: TCP
      name: n8n
  selector:
    app: {{ .Release.Name }}-n8n
