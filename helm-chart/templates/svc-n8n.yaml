apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-n8n-svc
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.n8n.env.N8N_PORT }}
      targetPort: {{ .Values.n8n.env.N8N_PORT }}
      protocol: TCP
      name: n8n
  selector:
    app: {{ .Release.Name }}-n8n
